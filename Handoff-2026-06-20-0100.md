---
author: pi (Claude Code)
type: handoff
created: 2026-06-20 01:00
session_topic: Groq→local Whisper switch
tags: [handoff, session-log, whisper, transcription]
---

# Handoff: Switch Groq Whisper to self-hosted faster-whisper

## What Was Done
- Created `internal/adapters/transcription/local.go` — new Whisper adapter pointing to `http://whisper:8000/v1/audio/transcriptions`
- Deleted `groq.go` and `groq_test.go` — removed all Groq API code
- Config: `GroqAPIKey` → `WhisperBaseURL` (env `WHISPER_BASE_URL`)
- Updated all docs, env files, and startup skill
- Deployed to prod (4 commits)

## What Changed (Files)
- `internal/adapters/transcription/local.go` + `local_test.go`: NEW self-hosted Whisper adapter
- `internal/adapters/transcription/groq.go`, `groq_test.go`: DELETED
- `internal/config/config.go`: `GroqAPIKey` field → `WhisperBaseURL`
- `cmd/bot/main.go`: `NewGroqAdapter(cfg.GroqAPIKey)` → `NewLocalAdapter(cfg.WhisperBaseURL)`
- `.env`, `.env.example`, `.env.test`, `.env.test.example`: GROQ_API_KEY removed, WHISPER_BASE_URL added
- `internal/delivery/telegram/handlers/booking_file.go`: Added error logging for transcription failures + entry debug log (`HandleFileMessage called`)

## Unfinished / In Progress
- **❌ Transcription not working**: Voice messages received and saved, but never transcribed. `HandleFileMessage` handler DOES NOT APPEAR to fire (entry log never seen). Whisper containers receive no requests from massage-bot.
- The user confirmed they are sending to `@vera_massage_bot`. Booking flow and other handlers work fine.

## Critical Context for Next Session
- Both `whisper` (172.22.0.14) and `whisper-2` (172.22.0.18) containers are on `caddy-test-net`. Both use `fedirz/faster-whisper-server:latest-cpu` (20-month-old image), identical code. The massage-bot container is also on `caddy-test-net`.
- `WHISPER_BASE_URL=http://whisper:8000/v1/audio/transcriptions` is set in `/opt/vera-bot/.env`
- The adapter (local.go) now matches `agentic-lab-2.0`'s `connect_handler.go` implementation exactly (same model name, same io.ReadAll+json.Unmarshal pattern).
- Direct Python test from within whisper-2 container confirms the endpoint works (200 OK with `{"text":"you"}`).
- **Most important**: The handler entry log `HandleFileMessage called` never appears, suggesting `telebot.OnVoice` is not triggering. This needs investigation separately from the whisper API integration.

## Decisions Made
- Removed Groq API key entirely (not kept as fallback) — user explicitly requested to delete "everything related to it"
- Matched agentic-lab-2.0 adapter code exactly to ensure compatibility — both use same image and endpoint
- Added entry log in `booking_file.go` before any processing to help diagnose handler-not-firing issue

## Session Attempts
1. Deploy 1 (123bfad): `whisper-1` model, `ru` language, streaming reader → 400 Bad Request
2. Deploy 2 (cce200a): Added error logging in booking_file.go → still 400 (no log shown)
3. Deploy 3 (4a89d7b): Matched agentic-lab pattern (model name, io.ReadAll, no language) → still 400
4. Deploy 4 (ccfa780): Added entry log in HandleFileMessage → NO LOG APPEARS
# 📔 Инструкция для Веры: Управление клиникой (v5.0.0)

Привет, Вера! Это руководство поможет тебе настроить работу с медицинскими картами на iPhone и управлять ботом с помощью административных команд.

---

## 📱 Работа с картами через Obsidian

Все записи о пациентах автоматически сохраняются в формате заметок, которые ты можешь редактировать прямо на телефоне.

### Шаг 1: Настройка Obsidian

1. Установи приложение **Obsidian** из App Store.
2. Создай новое хранилище (Vault) с названием **Пациенты**.
3. В настройках (шестеренка) → **Community plugins** → включи плагины.
4. Установи и включи плагин **Remotely Save**.

### Шаг 2: Подключение к боту (WebDAV)

В настройках плагина **Remotely Save** выбери **WebDAV** и введи данные:

- **Server Address:** `https://vera-bot.kfilin.icu/webdav/`
- **Username:** `vera`
- **Password:** *(твой секретный токен из .env)*

> [!TIP]
> Нажимай значок "две стрелочки" в Obsidian, чтобы синхронизировать свои заметки с ботом. Робот сам создаст файлы для новых пациентов по шаблону.

---

## 📅 Управление расписанием (v5.0)

Бот напрямую связан с твоим Google Календарем. В версии 5.0 мы перешли на новую систему **Free/Busy API**, которая работает еще точнее.

1. **Записи пациентов**: Появляются в календаре автоматически. В описании записи будет указано имя пациента и выбранная услуга.
2. **Личное время и блокировки**: Если тебе нужно перекрыть время (обед, тренировка, личные дела), просто создай событие в Google Календаре.
   - Бот видит **любые** занятые блоки времени в твоем календаре и автоматически убирает их из доступных для записи.
3. **Отмена со стороны Веры**: Если ты удалишь запись пациента в календаре, бот это увидит, но **не отправит** автоматическое уведомление пациенту (это нужно сделать вручную в Telegram).

---

## 💾 Автоматические бэкапы 2.0 (v5.0)

Теперь твои данные находятся под тройной защитой:

- **Раз в 24 часа**: Бот автоматически собирает все медицинские карты и базу данных в один защищенный архив.
- **Доставка в Telegram**: Готовый архив бот присылает напрямую Кириллу в Telegram.
- **Ручной бэкап**: Ты можешь в любой момент написать команду `/backup`, и бот мгновенно пришлет тебе свежую копию всех документов.

---

## 🛠 Команды администратора

Эти команды работают только для тебя и Кирилла. Просто напиши их в чат с ботом.

- `/status` — Проверка состояния систем (база данных, Google Calendar, WebDAV).
- `/backup` — Создать мгновенную копию базы данных и всех документов.
- `/ban [ID или username]` — Блокировка пользователя. Бот будет вежливо отвечать "Мест нет".
- `/unban [ID или username]` — Снятие блокировки.

---

## 💬 Напоминания и общение

### 🤖 Автоматические напоминания

Бот сам напоминает пациентам о записях за **72 часа** и за **24 часа**.

- Когда пациент нажимает "Подтвердить", ты получишь сообщение: *"✅ Запись подтверждена!"*.

### 📥 Входящие сообщения

Если пациент пишет боту или присылает файл, бот пересылает его тебе с кнопкой **[ ✍️ Ответить ]**.

- Нажми «Ответить», введи текст и отправь. Бот перешлет твой ответ пациенту и **автоматически сохранит** всю переписку в его мед-карту.

---

## 📂 Организация файлов

Бот автоматически раскладывает документы по папкам:

- `scans/` — Снимки МРТ, КТ, анализы.
- `images/` — Фотографии процесса.
- `messages/` — Расшифровки голосовых сообщений и переписка.

> [!IMPORTANT]
> Ты можешь переименовывать папки в Obsidian, главное — **не удаляй ID в скобках** (например, `(12345)`). По этому ID бот находит пациента.

---
*Версия системы: 5.0.0 Stable (Техническое совершенство).*
